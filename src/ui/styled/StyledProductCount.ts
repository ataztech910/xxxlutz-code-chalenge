import styled from 'styled-components';
import { ProductCount } from "../ProductCount";
import {colors} from "../shared/colors";

export const StyledProductCount = styled(ProductCount)`
  color: ${colors.softGray};
  font-size: 12px;
`;
